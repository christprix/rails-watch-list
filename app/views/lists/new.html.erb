<h1>New List</h1>
<%= simple_form_for @list do |f|%>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Submit", class: 'btn btn-primary' %>
<% end %>
<div>
  <%= link_to "Back to Lists", lists_index_path, class: 'btn btn-primary my-3' %>
</div>
